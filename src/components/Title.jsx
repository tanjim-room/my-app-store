import React from 'react';

const Title = ({title}) => {
    return (
        <div>
            <h2 className='text-4xl text-center font-boldpy-4'>{title}</h2>
        </div>
    );
};

export default Title;